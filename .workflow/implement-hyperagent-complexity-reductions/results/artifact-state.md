# Artifact, State, Safety Result

Implemented:
- Added artifact metadata fields to mission, proposal, Forge review, and decision templates.
- Added the same artifact metadata to generated mission/proposal/Forge/decision files.
- Updated UI proposal status parsing to prefer explicit artifact status and decision fields instead of broad accepted/rejected word matching.
- Added state ownership rules to `hyperagent/capability-registry.md` and `workshop/backlog.md`.
- Added `docs/safety-policy.md` and linked it from security/contributing docs.
- Added `docs/adapters.md` to define the adapter boundary without implementing non-Codex adapters.

Opportunities covered:
11, 17, 18, 31, 32, 33, 34, 43, 44, 45.
